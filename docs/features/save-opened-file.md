# 保存已打开文件

> 状态：实现中

## 背景与目标

Textora 已能打开并编辑本地文本文件，Rust 文档核心也已具备严格编码、换行规范化、冲突检测和原子替换能力，但两者尚未通过 IPC 与前端会话连接。本功能交付下一个最小垂直切片：用户可以把当前已打开文件的修改安全保存回原路径，并在失败时继续保有编辑内容和未保存状态。

## 范围

- 为当前已打开且已修改的文件提供清晰可发现的普通保存入口。
- 通过受限 Tauri 命令把当前 Unicode 内容交给 Rust 保存核心，沿用打开时的路径、编码、换行、指纹和只读信息。
- 文本内容使用二进制 IPC 传输，不进入大型 JSON 字符串或数字数组。
- 保存成功后更新文件指纹、字节数及会话状态，并清除未保存标记。
- 映射并展示大小超限、只读、Mixed 未选择换行、不可编码、编码歧义、外部修改冲突和一般 I/O 失败。
- 保存期间协调编辑、打开和重复保存操作，避免成功结果覆盖更新的会话状态。

## 非范围

- 新建文档首次保存、另存为、改名、移动文件和显式编码转换。
- Mixed 换行的 LF/CRLF 选择界面。
- 冲突后的重新加载、强制覆盖或取消选择；本切片只安全拒绝并说明原因。
- 关闭未保存文档保护、多标签、多窗口、自动保存、备份和持续外部修改监听。
- 文件菜单、系统菜单和键盘快捷键；本切片只要求一个可发现的保存入口。

## 用户流程

1. 用户打开本地文件并修改内容。
2. 用户触发“保存”，应用进入保存状态并阻止并发文件操作。
3. 前端以文档 ID 和二进制 UTF-8 内容请求后端保存；后端从可信会话状态取得路径及打开时元数据。
4. Rust 保存核心完成编码、校验和原子替换。
5. 成功时应用更新描述信息、清除未保存状态并继续显示当前内容；失败时显示可理解的错误，内容和未保存状态保持不变。

## 行为规则与边界情况

- 只有已打开且有路径的文档可普通保存；内存新文档不得静默创建文件，保存入口应禁用或明确提示需要后续“另存为”能力。
- 未修改文档触发保存时不得写盘；可直接保持当前状态。
- 只读文档不得进入写入流程；后端仍必须校验只读状态，不能只依赖前端禁用按钮。
- 同一窗口同时只允许一个打开或保存流程。保存期间编辑器只读，并禁用打开与重复保存；完成或失败后恢复。
- 保存请求必须绑定后端已知的文档 ID。前端不得提交任意文件路径，也不得自行决定可信指纹；未知或过期 ID 必须拒绝。
- 后端应保存当前文档的路径、编码、换行、指纹和只读信息，并在每次成功保存后原子更新指纹与字节数，供连续保存使用。
- 成功只能对应本次提交的完整内容；在成功响应前不得清除未保存标记或修改会话描述信息。
- 任一保存错误都不得改变编辑器内容或清除未保存状态；临时文件和磁盘原文件的保护继续由 Rust 保存核心保证。
- 外部修改冲突必须拒绝覆盖并给出明确提示；强制覆盖与重新加载留待后续功能。
- Mixed 换行、GBK 不可编码内容或 `EncodingAmbiguous` 必须明确拒绝；提示可说明需要后续选择换行或另存为 UTF-8，但不得展示当前尚不存在的可执行操作。
- 错误使用稳定代码与必要的安全字段映射，不向前端或用户暴露内部路径、临时文件名或 Rust 调试文本。
- 前端不得因此获得宽泛文件系统、shell、dialog 或网络权限。

## 验收条件

- [ ] 已打开的 UTF-8、UTF-8 BOM 和可无损重开的 CP936 文件修改后可保存到原路径，磁盘内容、编码与换行正确。
- [x] 保存成功后未保存标记清除，新的字节数和指纹用于下一次连续保存。
- [x] 文本内容经二进制 IPC 传输；保存命令按后端文档 ID 解析目标，前端不能提交任意路径。
- [x] 新建文档、未修改文档和只读文档不会被普通保存错误写入。
- [x] 保存期间编辑器、打开入口和重复保存受到保护，结束后恢复可用。
- [x] 大小超限、Mixed、不可编码、编码歧义、外部修改和一般写入失败均保留当前内容与未保存状态，并显示稳定、可理解的提示。
- [x] 外部修改冲突不会覆盖磁盘变化；失败不会被误报为已保存。
- [x] 前端未获得新增的宽泛文件系统、shell、dialog 或网络权限。
- [x] 自动化测试覆盖后端状态绑定与错误映射、前端保存状态机及成功/失败会话更新；交互式 macOS 真实文件保存验收与 Windows 验证待执行并在记录中明确。

## 依赖与约束

- 属于 `docs/features/basic-text-editing.md` 的保存子切片。
- 复用已完成的 `docs/features/safe-save-core.md`，不得在 IPC 或前端重复实现编码、大小限制、冲突检测或原子替换。
- 遵守 `docs/ARCHITECTURE.md` 的 Rust 文档核心、文档权威源、二进制 IPC 和最小权限不变量。
- 编码规则由 D-005 与 D-006 约束；跨平台行为由 D-003、D-004 约束。

## 开放问题

暂无会阻止本切片实现的问题。另存为、编码转换、Mixed 换行选择和冲突解决分别进入后续规格。

## 验证记录

实现与自动化验证已完成（2026-07-21），交互式 macOS 真实保存验收与 Windows 验证待执行。

实现要点：

- **后端文档状态**：`OpenBuffer` 升级为 `DocumentStore`，同时维护当前可信文档与候选打开的内容/元数据。`select_and_open_document` 只写入候选；`read_document_content` 按正确 id 成功取回内容时才在同一临界区提升为当前文档，因此内容读取失败不会使界面保留的旧文档失去保存关联。保存成功后更新当前指纹与字节数供连续保存。
- **二进制保存 IPC**：新增异步 `save_document(request: tauri::ipc::Request, state)`。内容经 Raw body 传输（前端 `invoke("save_document", uint8array, { headers: { "textora-document-id": id } })`），id 经自定义 header 随行；后端按 id 在可信状态中解析目标，前端不提交任意路径或指纹。编码、文件读取/写入与 `sync_all` 通过 `spawn_blocking` 离开 Tauri 命令线程，并复用 `safe-save-core`。
- **错误契约**：打开与保存分别映射核心错误；保存新增 `save-failed`，`unencodable-content` 携带 `character`/`byteOffset`。前端保留完整保存错误信封，`describeSaveError` 使用保存语境文案并展示不可编码字符的 Unicode 码点与 UTF-8 字节偏移，不泄露内部路径或调试文本。
- **前端会话**：`DocumentSession` 增加 `saveStatus`/`saveError` 与 `isBusy`/`requestSave`/`failSave`/`cancelSave`/`commitSavedDocument`；`App.tsx` 增加 Save 入口（仅 path 非空、已修改、非只读、非忙碌时可用）、保存流水线、保存期间编辑器/打开/重复保存互斥、成功清脏与失败保留的稳定提示。

已执行（macOS，stable 工具链）：

- `cargo fmt --manifest-path src-tauri/Cargo.toml --check` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过（**71 passed / 0 failed**）。IPC 覆盖打开/保存错误分流、`save-failed`、`UnencodableContent` 字符与偏移、候选打开在内容成功取回前不替换当前文档、按 id 更新指纹及拒绝未知 id。
- `npm run check` 通过（typecheck + vitest **35 passed / 0 failed**）。覆盖保存状态机、完整错误信封保留、保存专用文案与不可编码字符定位、平台错误守卫、App 保存入口的禁用与忙碌互斥。
- `npm run tauri -- build` 通过并生成 `Textora.app`；`capability` 仍仅 `core:app:default`，未新增 dialog/fs/shell/网络权限。
- 打开 `Textora.app` 确认进程启动（macOS）。
- `git diff --check` 通过。
- Clippy 未运行：stable 工具链缺 clippy 组件。

平台/交互待验证：

- 交互式 macOS 真实保存验收：打开 UTF-8/UTF-8 BOM/CP936 文件 → 编辑 → Save → 确认磁盘内容/编码/换行正确、未保存标记清除、外部修改/只读/超限/不可编码/歧义被明确拒绝且原文档不变。当前自动化覆盖对应逻辑，但端到端原生交互需人工执行。
- Windows 10 22H2+ / Windows 11 的 Raw body + header 传输、保存与原子替换行为需在对应环境确认。
