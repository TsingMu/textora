@AGENTS.md

# Claude Code 专用规则

## 当前任务快捷入口

- 当用户说“实现当前任务”或直接输入 `/current-task` 时，必须使用项目级 `current-task` Skill：若存在“进行中”任务则恢复它，否则启动 `docs/tasks/current.md` 中第一项“已承诺待办”。
- 当用户说“继续当前任务”时，只允许恢复已经处于“进行中”的任务；若不存在则停止并报告，不得自动选择待办。
- 当用户说“开始下一任务”时，仅在不存在“进行中”任务时启动第一项“已承诺待办”；存在进行中任务时停止并报告冲突。
- 当用户说“验收当前任务”时，不新增计划外行为，只按任务完成标准执行验证、修复阻塞验收的范围内问题并更新真实状态。
- “继续下一步”含义不明确。收到该表达时按“实现当前任务”处理：优先恢复进行中任务，否则启动第一项已承诺待办。
- 一次调用最多完成一个 Current Task。任务完成后必须停止并交接，不得自动开始下一任务、从 Backlog 选任务或自行扩大 Feature Spec。

## Compact Instructions

压缩会话时，摘要必须保留以下内容：

- 当前任务名称、状态、目标、范围和非范围；
- 已完成与剩余工作的准确边界，以及唯一的下一动作；
- 本次修改文件、用户或前序会话已有改动及二者的归属；
- 无法直接从代码恢复的关键决策、约束及采用当前方案的原因；
- 已执行验证的完整命令、退出结果、失败原因和仍需运行的验证；
- 仍需使用的 `/private/tmp` 日志或其他临时产物路径；
- 当前阻塞、风险、待确认事项及禁止自动进入下一任务的约束。

压缩摘要不保留完整正常日志、完整文件内容、大段 diff、已经排除的探索路径或可从代码和 Git 直接恢复的普通实现细节。压缩后以 `docs/tasks/current.md`、工作区文件和 Git 差异为事实源；若摘要与磁盘状态冲突，先核对并报告，不得猜测或覆盖。

## 工具输出预算

工具输出一旦进入会话就会在后续请求中重复占用上下文。执行命令或读取文件前，先估计输出规模，并遵守以下规则。

### 默认上限

- 单次工具输出默认不超过 120 行或约 12 KiB；预计超过时必须先缩小范围或把完整输出写入 `/private/tmp` 下的任务专用临时日志。
- 成功命令只读取足以确认结果的摘要；失败命令最多读取约 160 行或 20 KiB 的相关错误、失败用例和末尾上下文。
- 不为了“以防万一”打印完整文件、完整测试日志、完整仓库 diff、递归目录树、锁文件、生成文件、压缩/二进制内容、base64 或大段 JSON。
- 不得输出环境变量、认证信息、密钥或其他敏感配置；检查配置时只读取明确需要且可安全展示的字段。

### 搜索与文件读取

- 文件发现和文本搜索优先使用带路径与 glob 约束的 `rg --files`、`rg -n`；不要从仓库根目录无过滤地递归列出。
- 已知目标文件时使用 `sed -n` 或等价的行范围读取；长文件先搜索标题、符号或关键字，再读取命中附近的小范围。
- 同一文件未变化时不要反复完整读取；后续只读取修改区、相关符号或必要行段。
- 多个独立目标可并行搜索，但每个结果都要限制范围；不要把多份大输出合并到一次工具结果中。

### 测试、构建与日志

- 先运行与当前修改直接相关的定向测试；只有达到收尾条件后才运行约定的完整检查、构建或 release 验证。
- 预计输出较大的测试或构建应把完整 stdout/stderr 写入 `/private/tmp` 下的唯一临时日志，不使用会同时回显全部内容的 `tee`。
- 命令成功时只报告命令、退出码和测试数量/关键摘要；命令失败时先用错误模式、失败用例名和日志末尾定位，再按需读取更窄的上下文。
- 不因日志被收起而弱化验证：必须保存真实退出码，失败时读取足够信息定位原因，最终只报告实际执行结果。
- 相同代码和配置未变化时，不重复运行同一昂贵完整检查；需要复验时说明触发复验的具体变化。

### Git 与差异检查

- 先用 `git status --short`、`git diff --stat` 或 `git diff --name-only` 确定范围，再按文件读取相关 diff。
- 除非任务确实需要全局审查，不输出整个仓库的完整 diff；对大型或生成文件只报告路径与变更规模。
- 保留并避开用户已有的无关改动，不通过大段 diff 输出把无关内容带入上下文。

### 子代理与最终报告

- 若使用子代理处理搜索、测试日志或代码审查，要求其返回简短结论、精确文件/行号和必要验证，不返回原始大日志或整段文件。
- 若受限输出不足以判断，继续缩小问题范围并读取针对性片段；只有无法通过过滤诊断时才逐步提高上限，禁止一次性倾倒全部内容。
- 最终报告保留可复现的命令和真实结果，不粘贴完整正常日志。
